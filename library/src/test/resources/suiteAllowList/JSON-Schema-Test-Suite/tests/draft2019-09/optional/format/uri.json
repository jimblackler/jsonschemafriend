{
  "validation of URIs" : {
    "an invalid protocol-relative URI Reference" : true,
    "an invalid relative URI Reference" : true,
    "an invalid URI" : true,
    "an invalid URI though valid URI reference" : true,
    "an invalid URI with spaces" : true,
    "an invalid URI with spaces and missing scheme" : true,
    "an invalid URI with comma in scheme" : true
  }
}