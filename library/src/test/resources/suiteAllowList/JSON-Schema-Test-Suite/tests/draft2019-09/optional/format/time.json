{
  "validation of time strings" : {
    "invalid leap second, Zulu (wrong hour)" : true,
    "invalid leap second, Zulu (wrong minute)" : true,
    "invalid leap second, zero time-offset (wrong hour)" : true,
    "invalid leap second, zero time-offset (wrong minute)" : true,
    "invalid leap second, positive time-offset (wrong hour)" : true,
    "invalid leap second, positive time-offset (wrong minute)" : true,
    "invalid leap second, negative time-offset (wrong hour)" : true,
    "invalid leap second, negative time-offset (wrong minute)" : true,
    "an invalid time string with invalid hour" : true,
    "an invalid time string with invalid minute" : true,
    "an invalid time string with invalid second" : true,
    "an invalid time string with invalid leap second (wrong hour)" : true,
    "an invalid time string with invalid leap second (wrong minute)" : true,
    "an invalid time string with invalid time numoffset hour" : true,
    "an invalid time string with invalid time numoffset minute" : true,
    "an invalid time string with invalid time with both Z and numoffset" : true,
    "an invalid offset indicator" : true,
    "only RFC3339 not all of ISO 8601 are valid" : true,
    "no time offset" : true,
    "non-ascii digits should be rejected" : true
  }
}