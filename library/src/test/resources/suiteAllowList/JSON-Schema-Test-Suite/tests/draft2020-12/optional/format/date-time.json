{
  "validation of date-time strings" : {
    "a invalid day in date-time string" : true,
    "an invalid offset in date-time string" : true,
    "an invalid closing Z after time-zone offset" : true,
    "an invalid date-time string" : true,
    "only RFC3339 not all of ISO 8601 are valid" : true,
    "invalid non-padded month dates" : true,
    "invalid non-padded day dates" : true,
    "non-ascii digits should be rejected in the date portion" : true,
    "non-ascii digits should be rejected in the time portion" : true
  }
}