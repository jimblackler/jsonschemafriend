{
  "validation of JSON-pointers (JSON String Representation)" : {
    "not a valid JSON-pointer (~ not escaped)" : true,
    "not a valid JSON-pointer (URI Fragment Identifier) #1" : true,
    "not a valid JSON-pointer (URI Fragment Identifier) #2" : true,
    "not a valid JSON-pointer (URI Fragment Identifier) #3" : true,
    "not a valid JSON-pointer (some escaped, but not all) #1" : true,
    "not a valid JSON-pointer (some escaped, but not all) #2" : true,
    "not a valid JSON-pointer (wrong escape character) #1" : true,
    "not a valid JSON-pointer (wrong escape character) #2" : true,
    "not a valid JSON-pointer (multiple characters not escaped)" : true,
    "not a valid JSON-pointer (isn't empty nor starts with /) #1" : true,
    "not a valid JSON-pointer (isn't empty nor starts with /) #2" : true,
    "not a valid JSON-pointer (isn't empty nor starts with /) #3" : true
  }
}