plugins {
    id 'java-gradle-plugin'
    id 'maven-publish'
}

group = 'com.github.jimblackler'
version = '0.2'

dependencies {
    implementation project(path: ':codegen')
}

gradlePlugin {
    plugins {
        jsonSchemaTypes {
            id = 'jsonschematypes'
            implementationClass = 'net.jimblackler.jsonschematypes.plugin.JsonSchemaTypesPlugin'
        }
    }
}
